# Go API
apiVersion: apps/v1
kind: Deployment
metadata:
  name: swing-detector-api
  labels:
    app.kubernetes.io/name: swing-detector-api
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: swing-detector-api
  template:
    metadata:
      labels:
        app.kubernetes.io/name: swing-detector-api
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8000"
        prometheus.io/path: "/health"
    spec:
      initContainers:
        - name: migrate
          image: ghcr.io/6cclab/swing-detector-api:latest
          command: ["/migrate", "-direction", "up", "-path", "file:///migrations"]
          envFrom:
            - secretRef:
                name: swing-detector-secrets
          resources:
            requests:
              cpu: 50m
              memory: 32Mi
            limits:
              cpu: 200m
              memory: 64Mi
      containers:
        - name: api
          image: ghcr.io/6cclab/swing-detector-api:latest
          ports:
            - containerPort: 8000
              protocol: TCP
          envFrom:
            - secretRef:
                name: swing-detector-secrets
          env:
            - name: PORT
              value: "8000"
            - name: VIDEO_STORAGE_PATH
              value: /app/uploads
            - name: REDIS_URL
              value: redis://swing-detector-redis:6379
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 2
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 256Mi
          volumeMounts:
            - name: uploads
              mountPath: /app/uploads
          securityContext:
            runAsNonRoot: true
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          lifecycle:
            preStop:
              exec:
                command: ["sleep", "30"]
      volumes:
        - name: uploads
          persistentVolumeClaim:
            claimName: swing-detector-uploads
---
# Python ML Worker
apiVersion: apps/v1
kind: Deployment
metadata:
  name: swing-detector-worker
  labels:
    app.kubernetes.io/name: swing-detector-worker
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: swing-detector-worker
  template:
    metadata:
      labels:
        app.kubernetes.io/name: swing-detector-worker
    spec:
      containers:
        - name: worker
          image: ghcr.io/6cclab/swing-detector-worker:latest
          envFrom:
            - secretRef:
                name: swing-detector-secrets
          env:
            - name: VIDEO_STORAGE_PATH
              value: /app/uploads
            - name: MODEL_CHECKPOINT_DIR
              value: /app/checkpoints
            - name: REDIS_URL
              value: redis://swing-detector-redis:6379
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: "2"
              memory: 2Gi
          volumeMounts:
            - name: uploads
              mountPath: /app/uploads
            - name: checkpoints
              mountPath: /app/checkpoints
          securityContext:
            runAsNonRoot: true
            readOnlyRootFilesystem: false
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
      volumes:
        - name: uploads
          persistentVolumeClaim:
            claimName: swing-detector-uploads
        - name: checkpoints
          persistentVolumeClaim:
            claimName: swing-detector-checkpoints
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: swing-detector-uploads
spec:
  accessModes: ["ReadWriteMany"]
  resources:
    requests:
      storage: 10Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: swing-detector-checkpoints
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 2Gi
