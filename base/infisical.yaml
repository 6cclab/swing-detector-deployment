apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: swing-detector
spec:
  resyncInterval: 60
  authentication:
    kubernetesAuth:
      identityId: ccebb465-42d4-4b19-9dac-9eec7a9e5613
      serviceAccountRef:
        name: infisical-auth
      secretsScope:
        projectId: ""  # set in env overlay or before deploy
        envSlug: dev
        secretsPath: /
        recursive: true
  managedSecretReference:
    secretName: swing-detector-secrets
    creationPolicy: Owner
